@using EntityLayer.Concrete
@model List<Message2>

<li class="nav-item dropdown">
    <a class="nav-link count-indicator dropdown-toggle" id="messageDropdown" href="#" data-toggle="dropdown" aria-expanded="false">
        <i class="mdi mdi-email-outline"></i>
        @if ( ViewBag.unreadCount > 0 )
        {
            <span class="count-symbol bg-success"></span>

        }else if( ViewBag.readCount > 0 )
        {
            <span class="count-symbol bg-warning"></span>

        }
    </a>
    <div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="messageDropdown">
        <h6 class="p-3 mb-0">Messages</h6>

        @if (Model == null || !Model.Any())
        {
            <div class="dropdown-divider"></div>
            <div class="text-center p-3">
                <p class="text-muted">Tümü okundu veya mesaj yok</p>
            </div>
        }

        @foreach (var item in Model)
        {
            <div class="dropdown-divider"></div>
            <a href="/Message/Inbox/" class="dropdown-item preview-item">
                <div class="preview-thumbnail">
                    <img src="~/WriterImageFiles/@item.SenderUser.WriterImage" alt="image" class="profile-pic">
                </div>
                <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">
                    <h6 class="preview-subject ellipsis mb-1 font-weight-normal">@item.SenderUser.WriterName Sana Mesaj Gönderdi</h6>
                    <p class="text-gray mb-0">@(((DateTime)item.MessageDate).ToString("dd-MMM-yyyy"))</p>
                </div>
            </a>
        }
        @* <div class="dropdown-divider"></div>
        <a class="dropdown-item preview-item">
        <div class="preview-thumbnail">
        <img src="~/writer/writer/assets/images/faces/face2.jpg" alt="image" class="profile-pic">
        </div>
        <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">
        <h6 class="preview-subject ellipsis mb-1 font-weight-normal">Cregh send you a message</h6>
        <p class="text-gray mb-0"> 15 Minutes ago </p>
        </div>
        </a>
        <div class="dropdown-divider"></div>
        <a class="dropdown-item preview-item">
        <div class="preview-thumbnail">
        <img src="~/writer/writer/assets/images/faces/face3.jpg" alt="image" class="profile-pic">
        </div>
        <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">
        <h6 class="preview-subject ellipsis mb-1 font-weight-normal">Profile picture updated</h6>
        <p class="text-gray mb-0"> 18 Minutes ago </p>
        </div>
        </a>
        <div class="dropdown-divider"></div>
        <h6 class="p-3 mb-0 text-center">4 new messages</h6> *@
    </div>
 </li>

