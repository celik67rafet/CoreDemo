@using EntityLayer.Concrete

@model List<Notification>

<div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="notificationDropdown">
    <h6 class="p-3 mb-0">Bildirimler</h6>
    @foreach( var item in Model.Where( x => x.NotificationStatus == true ).OrderByDescending( x => x.NotificationID ).Take(5))
    {
            <div class="dropdown-divider"></div>
            <a class="dropdown-item preview-item">
                <div class="preview-thumbnail">
                    <div class="@item.NotificationColor">
                        <i class="@item.NotificationTypeSymbol"></i>
                    </div>
                </div>
                <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">
                    <div class="d-flex flex-row">
                    <h6 class="preview-subject font-weight-normal mb-1" style="margin-right:0.2em;">@item.NotificationType |</h6><p style="font-size:0.7em;">@(((DateTime)item.NotificationDate).ToString("dd-MMM-yyyy"))</p>
                    </div>
                    <p class="text-gray ellipsis mb-0">@item.NotificationDetails</p>
                </div>
            </a>
    }
    @* <div class="dropdown-divider"></div>
    <a class="dropdown-item preview-item">
        <div class="preview-thumbnail">
            <div class="preview-icon bg-warning">
                <i class="mdi mdi-settings"></i>
            </div>
        </div>
        <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">
            <h6 class="preview-subject font-weight-normal mb-1">Settings</h6>
            <p class="text-gray ellipsis mb-0"> Update dashboard </p>
        </div>
    </a>
    <div class="dropdown-divider"></div>
    <a class="dropdown-item preview-item">
        <div class="preview-thumbnail">
            <div class="preview-icon bg-info">
                <i class="mdi mdi-link-variant"></i>
            </div>
        </div>
        <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">
            <h6 class="preview-subject font-weight-normal mb-1">Launch Admin</h6>
            <p class="text-gray ellipsis mb-0"> New admin wow! </p>
        </div>
    </a> *@
    <div class="dropdown-divider"></div>
    <a href="/Notification/AllNotification/"><h6 class="p-3 mb-0 text-center">Tüm Bildirimleri Göster</h6></a>
</div>